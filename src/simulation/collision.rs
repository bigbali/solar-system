pub fn collosion_system() {
    not_implemented!()

    // if bodies collide, release energy
    // ? negative mass maybe?
    // determine released energy
    // if energy large enough, destroy one or both bodies
    // body applies force to other body, pushing it away
}
